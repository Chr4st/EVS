# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

EVS — (description TBD).

## Build & Test Commands

<!-- Update these once the project has a build system -->
<!-- Example:
- Build: `make build`
- Test all: `make test`
- Test single: `make test TEST=path/to/test`
- Lint: `make lint`
-->

## Architecture

<!-- Document high-level architecture once code is added -->
